apply plugin: "application"
mainClassName = "ir.haji.nutro.gateway.GatewayApplication"
def springVersion = "2.3.1.RELEASE"

dependencies {
    compile "org.springframework.cloud:spring-cloud-starter-zuul:1.0.3.RELEASE"
}